// import Produto from '../../entities/Produto.js.js';

class AtualizarEstoque {
  constructor(estoqueRepository) {
    this.estoqueRepository = estoqueRepository;
  }

  async execute(id, quantidade) {
    return("Ainda não implmentado!")
  }
}

export default AtualizarEstoque;